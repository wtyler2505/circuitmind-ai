import { describe, it } from 'vitest';

describe.skip('server route validation behavior', () => {
  it('is covered by server/tests/route-validation.test.ts', () => {
    // Intentionally skipped in frontend Vitest environment.
  });
});
